# To install and use HELM Charts

```bash
    helm install <package> .
```

Make sure you have proper values in the Values.yaml file
---

# To go into the templates/ folder and run

```bash
    kubectl apply -f .
```